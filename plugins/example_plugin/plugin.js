// Adding a new command to the bot is as easy as defining another function.
// Each command must be able to take 3 arguments, the bot itself, the message object, and the arguments to the command.

// For example, the following code defines a new command, which will be run when a user types in "!ping",
// and the bot will respond with "pong!"

// module.exports.ping = function(bot, message, args) {
//     bot.sendMessage(message, "pong!");
// }

// If any external dependencies need to be installed, then run `npm install --save <dependency>` in the plugin directory.
